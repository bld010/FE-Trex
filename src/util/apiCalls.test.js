import {
  fetchMyTrips
} from './apiCalls';

describe('apiCalls', () => {

  describe('fetchMyTrips', () => {

    it.only('should do a thing', () => {
      expect(true).toEqual(true)
      
    })
  })

})